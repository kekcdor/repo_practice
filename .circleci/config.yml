version: 2.1
jobs:
  build:
    working_directory: ~/project
    docker:
      - image: cimg/openjdk:19.0.0
    steps:
      - checkout
      - run: mvn -U clean install -DskipTests
      - run: mvn test